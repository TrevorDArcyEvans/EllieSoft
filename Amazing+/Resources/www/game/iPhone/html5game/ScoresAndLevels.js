function JS_GetHighScore()
{
  return Number(localStorage.HighScore);
}

function JS_SetHighScore(val)
{
  localStorage.HighScore = val;
}

function JS_GetHighLevel()
{
  return Number(localStorage.HighLevel);
}

function JS_SetHighLevel(val)
{
  localStorage.HighLevel = val;
}

